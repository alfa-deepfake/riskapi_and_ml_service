# Host-specific runtime config — never commit (forces its ports on every host).
.env
docker-compose.override.yml

# Python bytecode
__pycache__/
*.pyc

# Large model files are stored through Git LFS; their tracking rules are in
# .gitattributes. Small v15 XGB trees, configs, and the CNN calibrator remain
# regular Git files.

# Downloaded at deploy time (scripts/download_runtime_models.sh), never committed.
models/insightface/
v16_release.tar.gz
